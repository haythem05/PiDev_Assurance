{% extends 'base.html.twig' %}
{% block content %}
<header class="section-header">
          <div id="google_translate_element"></div>
 <script type="text/javascript">
 function googleTranslateElementInit() {
new google.translate.TranslateElement({pageLanguage: 'fr'}, 'google_translate_element');
                        }
   </script>

   <script type="text/javascript" src="//translate.google.com/translate_a/element.js?cb=googleTranslateElementInit"></script></p>
        </header>
 <a href="{{ path('addCategorie') }}" class="btn btn-primary">Ajouter</a>
<div class="card">
<div class="card-body">
<h1 class="card-title"> Nos Catégories</h1>
<table class="table table-sm text-center table-bordered">
<thead>
    <tr>
        <th>Id</th>
        <th>Image</th>
         <th>Description</th>
        <th>Supprimer</th>
        <th>Modifier</th>
    </tr>
    </thead>
    <tbody>
    {% for i in categories %}
        <tr>
            <td>{{ i.id }}</td>
            {% if i.categorieImage %}
                <td><img src="{{ asset('uploads/images/categorie/' ~ i.categorieImage) }}" style="width:100px;height:100px;"></td>
            {% else %}
                <td>No image available</td>
            {% endif %}
             <td>{{ i.description }}</td>
            <td>
                <a href="{{ path('suppC', {'id': i.id}) }}"><i class="ri-delete-bin-2-fill" style="font-size: 1.5em;"></i></a>
            </td>
            <td>
                <a href="{{ path('updateCategorie', {'id': i.id}) }}"><i class="ri-pencil-fill" style="font-size: 1.5em;"></i></a>
            </td>
             
        </tr>
    {% endfor %}
    </tbody>
</table>
</div>
    </div>
{% endblock %}

